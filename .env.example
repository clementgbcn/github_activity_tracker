# GitHub Activity Tracker - Environment Configuration

# Required: Your GitHub Personal Access Token
# Create one at: https://github.com/settings/tokens
# Need at least 'repo' and 'read:user' scopes
GITHUB_TOKEN=your_github_personal_access_token_here

# Optional: Default GitHub organization to filter activities by
# If not specified, will need to be provided via --org parameter
# or use --no-org-filter to see activities across all organizations
GITHUB_ORG=optional_github_organization_name

# Optional: List of GitHub users to track (comma-separated)
# This is used if no --users or --users-file is specified
# Example: octocat,torvalds,gvanrossum
GITHUB_USERS=user1,user2,user3

# Optional: JIRA URL prefix for linking tickets (e.g., CTC-1234) found in PR titles
# Used to create clickable links in the HTML report
# Example: https://your-jira-instance.atlassian.net/browse/
JIRA_URL_PREFIX=https://jira.example.com/browse/

# Email configuration (optional)
# Set to true to enable email functionality with the --email flag
EMAIL_ENABLED=false

# Email sender configuration
# For Gmail, you might need to generate an "App Password" if 2FA is enabled
EMAIL_SENDER=your_email@example.com
EMAIL_PASSWORD=your_email_password_or_app_password
EMAIL_SMTP_SERVER=smtp.example.com
EMAIL_SMTP_PORT=587

# Comma-separated list of email recipients
EMAIL_RECIPIENTS=recipient1@example.com,recipient2@example.com

# Prefix for email subject lines
EMAIL_SUBJECT_PREFIX=[GitHub Activity]
